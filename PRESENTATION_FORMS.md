# اسکریپت ارائه فرم‌ها و دریافت داده از کاربر (Web Design)
 
**مدت زمان پیشنهادی:** 25–35 دقیقه  
**مخاطب:** درس طراحی صفحات وب  
**نویسنده / ارائه‌دهنده:** Amin Davodian (Mohammadamin Davodian)

**Short English note:**  
This file is a step-by-step speaking script for a 25–35 minute university talk about HTML forms and PHP.  
You can use it together with the Web Security Lab project and run the code either with the built-in PHP server  
(`php -S localhost:8000`) or via XAMPP (copy the project folder into `htdocs` and open `http://localhost/Tsw/dashboard.php`).

این اسکریپت دقیقاً بر اساس همین پروژه تنظیم شده و از فایل‌های زیر استفاده می‌کند.  
هر کدام از این فایل‌ها در طول ارائه در یک بخش مشخص استفاده می‌شوند:

- `01_get_post.php` → دمو تفاوت GET و POST و نمایش ساده `$_GET` / `$_POST`
- `02_validation.php` → مثال کامل‌تر Validation سمت سرور، همراه با پیام خطا و داده معتبر/نامعتبر
- `03_xss_demo.php` → دمو XSS بازتابی، نسخه‌ی ناامن و نسخه‌ی امن با `htmlspecialchars()`
- `final/register.php` + `final/profile.php` → تمرین نهایی ثبت‌نام و نمایش نتیجه در صفحه جدا، با جمع‌بندی مفاهیم قبلی

## 🔧 پیش‌نیازها و آماده‌سازی قبل از شروع

- **راه‌اندازی سرور PHP:**
  - در پوشه پروژه:
    - `php -S localhost:8000`
  - آدرس اصلی: `http://localhost:8000/dashboard.php`
- **تب‌های پیشنهادی در مرورگر:**
  - Tab 1: `dashboard.php` (برای معرفی پروژه)
  - Tab 2: `slides.html` (اگر خواستی اسلاید تمام‌صفحه نشان بدهی)
  - Tab 3: `01_get_post.php`
  - Tab 4: `02_validation.php`
  - Tab 5: `03_xss_demo.php`
  - Tab 6: `final/register.php`
- **فایل‌های باز در IDE / ادیتور:**
  - همین فایل `PRESENTATION_FORMS.md` برای روخوانی
  - در صورت نیاز: کدهای PHP مربوطه برای نشان دادن تکه‌کدها
- **نکته رفتاری:**
  - گاهی مکث کن، سوال بپرس، و اجازه بده دانشجوها جواب بدهند.
  - در دموها عجله نکن؛ هر مرحله را نام ببر ("الان دارم Submit می‌زنم... الان URL را چک می‌کنیم...").

---

## 1️⃣ مقدمه (۲–۳ دقیقه)

**متن پیشنهادی:**

```text
سلام به همگی 👋

امروز قراره یکی از مهم‌ترین بخش‌های طراحی صفحات وب رو با هم ببینیم:
«فرم‌ها و دریافت داده از کاربر».

تقریباً هر سایتی که باهاش کار می‌کنیم، یک جایی از ما چیزهایی می‌پرسه:
فرم ثبت‌نام، فرم لاگین، جستجو، ثبت سفارش و ...

ما امروز روی این سرفصل‌ها تمرکز می‌کنیم:
1) ارسال داده با GET و POST
2) دسترسی به داده‌ها با $_GET و $_POST
3) اعتبارسنجی داده‌ها (Validation)
4) جلوگیری از خطاها و حملات XSS
5) و در نهایت، ساخت یک فرم ثبت‌نام ساده و نمایش نتیجه در صفحه دیگر

همه این‌ها رو به صورت LIVE و روی همین پروژه با هم می‌بینیم. 😎
```

> **نکته برای ارائه:** این بخش را کوتاه، پرانرژی و تعاملی نگه دار. می‌توانی بپرسی:
> «چه کسی تا حالا فرم ثبت‌نام نوشته؟»

---

## 2️⃣ ارسال داده با GET و POST (۵–۷ دقیقه)

### 🎯 اهداف این بخش

- نشان دادن مسیر حرکت داده از فرم تا سرور
- توضیح تفاوت دیداری و رفتاری بین GET و POST
- آماده‌سازی ذهن دانشجو برای درک `$_GET` و `$_POST`
- اشاره به نکات امنیتی پایه (URL، history، HTTPS)

### 2.1. توضیح تئوری

**ایده اصلی:**

- **GET** → اطلاعات در URL دیده می‌شود (مثال: کارت پستال باز)
- **POST** → اطلاعات در بدنه درخواست است (مثال: نامه در پاکت بسته)

چند نکته مهم:

- GET برای جستجو، فیلتر، صفحه‌بندی خوب است
- اطلاعات حساس (رمز عبور، ایمیل) را با GET نفرستیم
- URL محدودیت طول دارد
- اطلاعات GET در history مرورگر و لاگ‌های سرور ذخیره می‌شود
- POST در URL نمایش داده نمی‌شود و برای فرم‌های حساس استفاده می‌شود

### 2.2. دمو با `01_get_post.php`

**Action:** صفحه `01_get_post.php` را باز کن.

**اسکریپت پیشنهاد‌شده:**

```text
اینجا دو تا فرم داریم: یکی با GET و یکی با POST.

اول می‌ریم سراغ GET:
یه کلمه مثل «لپ‌تاپ گیمینگ» می‌نویسم و Submit می‌کنم.

حالا نگاه کنید به آدرس بالا (URL):
می‌بینید که چیزی مثل ?query=لپ‌تاپ-گیمینگ به URL اضافه شده.

یعنی اگر این لینک رو برای یکی بفرستم، اون همون جستجوی من رو می‌بیند.
این برای سرچ و فیلتر خیلی خوبه.

حالا میام سراغ فرم POST.
اینجا یک رمز عبور تستی می‌نویسم و Submit می‌کنم.

می‌بینید که URL تمیزه، چیزی بهش اضافه نشده.
اما اگه DevTools رو باز کنیم و توی تب Network درخواست رو نگاه کنیم،
می‌بینیم که اطلاعات توی Request Payload ارسال شده.

نتیجه: POST امن‌تر از GET است، مخصوصاً وقتی با HTTPS ترکیب بشه.
اما هنوز باید سمت سرور هم درست باهاش برخورد کنیم.
```

### 2.3. ربط دادن به `$_GET` و `$_POST`

در همین صفحه، می‌توانی اشاره کنی:

- وقتی با GET ارسال می‌کنیم، داده‌ها در آرایه‌ی `$_GET` قرار می‌گیرند
- وقتی با POST ارسال می‌کنیم، داده‌ها در `$_POST` قرار می‌گیرند

```php
// مثال مفهومی (لازم نیست دقیقاً این را در کد بنویسی)
$nameFromGet  = $_GET['name']  ?? null;
$nameFromPost = $_POST['name'] ?? null;
```

> **نکته:** تأکید کن که همیشه باید قبل از استفاده، وجود کلید را چک کنیم (`isset` یا `??`).

---

## 3️⃣ دسترسی به داده‌ها با $_GET و $_POST (۳–۴ دقیقه)

اینجا می‌توانی خیلی کوتاه و کاربردی صحبت کنی:

- `$_GET` → مخصوص داده‌هایی است که با متد GET ارسال شده‌اند
- `$_POST` → مخصوص داده‌های متد POST
- `$_REQUEST` → ترکیب GET و POST و COOKIE (برای این درس فقط اشاره کوتاه کافی است)

**متن پیشنهادی:**

```text
خب، وقتی فرم رو Submit می‌کنیم، سوال اینه:
این داده‌ها از کجا وارد PHP می‌شن؟

جواب: PHP چند تا آرایه فوق‌العاده کاربردی داره:
- $_GET
- $_POST

اگر فرم ما method="GET" باشه، مقدارها داخل $_GET قرار می‌گیرند.
اگر method="POST" باشه، مقدارها داخل $_POST.

در همین صفحه GET/POST که دیدیم، قسمت دریافت داده‌ها دقیقاً از همین آرایه‌ها استفاده می‌کنه.
```

اگر استاد علاقه‌مند بود، می‌توانی داخل کد یک `var_dump($_GET)` یا `var_dump($_POST)` نشان بدهی (یا از خروجی موجود در پروژه استفاده کنی).

---

## 4️⃣ اعتبارسنجی داده‌ها (Validation) (۵–۷ دقیقه)

### 🎯 اهداف این بخش

- جا انداختن قانون «هرگز به ورودی کاربر اعتماد نکن»
- نشان دادن تفاوت داده معتبر و نامعتبر به صورت بصری
- توضیح رابطه‌ی Sanitization و Validation
- تأکید بر این‌که Validation واقعی باید در سمت سرور انجام شود

### 4.1. مفهوم Validation

**پیام اصلی:**

> «هیچ وقت به ورودی کاربر اعتماد نکن!»

چند مثال ساده:

- فیلد «نام» خالی ارسال شده
- به جای «سن»، متن ارسال شده
- ایمیل بدون `@`
- طول رشته خیلی کوتاه یا خیلی بلند

### 4.2. معرفی تابع پاک‌سازی (Sanitization) مفهومی

می‌توانی یک تابع مفهومی مثل `test_input` را روی اسلاید توضیح بدهی (نه لزوماً در کد پروژه):

```php
function test_input(string $data): string {
    $data = trim($data);              // حذف فاصله‌های ابتدا و انتها
    $data = stripslashes($data);      // حذف بک‌اسلش‌های اضافی
    $data = htmlspecialchars($data);  // جلوگیری از XSS در خروجی
    return $data;
}
```

اینجا توضیح بده که:

- `trim` برای زیبایی و جلوگیری از ورودی اشتباه
- `stripslashes` برای حذف کاراکترهای اضافه
- `htmlspecialchars` برای امن کردن خروجی در HTML

### 4.3. دمو با `02_validation.php`

**Action:** صفحه `02_validation.php` را باز کن.

**اسکریپت پیشنهادی:**

```text
اینجا یک فرم داریم با سه فیلد: نام، ایمیل، سن.

اول بدون نام ارسال می‌کنم → خطا می‌گیرم.
بعد نام خیلی کوتاه وارد می‌کنم (مثلاً 2 حرف) → خطا.

برای ایمیل، یک ایمیل ناقص وارد می‌کنم → خطای "فرمت ایمیل صحیح نیست".

برای سن، به جای عدد، حروف می‌نویسم → خطا.

حالا همه را درست پر می‌کنم.
می‌بینیم پیام موفقیت (✅) نمایش داده می‌شود و مقادیر با `htmlspecialchars` امن شده‌اند.

نکته مهم اینجاست: این Validation در **سمت سرور** انجام می‌شود.
حتی اگر جاوااسکریپت کاربر خاموش باشد، باز هم از داده نامعتبر جلوگیری می‌کنیم.
```

> **نکته برای تأکید:** سمت کلاینت (JavaScript) برای تجربه کاربری خوب است، ولی امنیت واقعی در سمت سرور است.

---

## 5️⃣ جلوگیری از XSS (Cross-Site Scripting) (۵–۷ دقیقه)

### 🎯 اهداف این بخش

- نشان دادن این‌که «چاپ ساده ورودی کاربر» می‌تواند به اجرای کد منجر شود
- توضیح تفاوت نسخه ناامن و نسخه امن در یک نگاه
- معرفی `htmlspecialchars()` به عنوان سپر اول دفاعی در PHP
- ربط دادن حمله XSS به دنیای واقعی (سرقت Cookie، تغییر ظاهر سایت، تزریق اسکریپت)

### 5.1. ایده‌ی حمله XSS

مثال:

```html
<script>alert('Hacked!');</script>
```

اگر این رشته را همان‌طور که هست در صفحه چاپ کنیم، جاوااسکریپت کاربر اجرا می‌شود.

می‌توانی با زبان ساده توضیح بدهی:

- دزدیدن Cookie
- تغییر ظاهر صفحه
- ریدایرکت کاربر به صفحه مخرب

### 5.2. دمو با `03_xss_demo.php`

**Action:** صفحه `03_xss_demo.php` را باز کن.

این صفحه دو بخش دارد:

- **روش ناامن (Vulnerable)**
- **روش امن (Secure)**

**اسکریپت پیشنهادی:**

```text
اول در فرم ناامن، این کد رو وارد می‌کنم:
<script>alert('Hacked!')</script>

Submit می‌کنم و می‌بینیم که Popup ظاهر می‌شه.
یعنی مرورگر این را به عنوان کد اجرا کرده، نه فقط متن.

حالا در فرم امن، همان کد را وارد می‌کنم.
این‌بار نتیجه فقط به صورت متن نمایش داده می‌شود.

چون در نسخه امن، از تابع htmlspecialchars در PHP استفاده شده.
این تابع کاراکترهای خطرناک مثل < و > را تبدیل می‌کند به &lt; و &gt;.
پس مرورگر دیگر آن را به عنوان کد جاوااسکریپت نمی‌شناسد.
```

### 5.3. نکته‌ی طلایی

**جمله‌ای که حتماً روی آن تأکید کن:**

> **برای هر چیزی که از کاربر می‌گیری و در HTML نمایش می‌دهی، `htmlspecialchars()` را یادت نرود.**

---

## 6️⃣ تمرین نهایی: فرم ثبت‌نام ساده (نام + ایمیل) و نمایش نتیجه در صفحه دیگر (۵–۷ دقیقه)

### 🎯 اهداف این بخش

- جمع کردن تمام مفاهیم قبلی در یک مثال واحد
- نشان دادن یک "مسیر کامل" از فرم تا صفحه نتیجه
- ربط دادن واضح سرفصل‌های استاد به کد واقعی (GET/POST، Validation، XSS)

برای این سرفصل می‌توانی از پوشه `final/` استفاده کنی که یک فرم ثبت‌نام نسبتاً کامل‌تر دارد. اگر استاد روی سادگی تأکید دارد، می‌توانی صرفاً روی فیلدهای «نام» و «ایمیل» تمرکز کنی و درباره‌ی رمز عبور خیلی وارد جزئیات نشوی.

### 6.1. ساختار سناریو

- صفحه فرم → `final/register.php`
- صفحه نمایش نتیجه → `final/profile.php`

### 6.2. دمو با `final/register.php`

**Action:** صفحه `final/register.php` را باز کن.

**اسکریپت پیشنهادی:**

```text
اینجا یک فرم ثبت‌نام داریم که حداقل شامل این فیلدهاست:
- نام و نام خانوادگی
- ایمیل
- (و در این نسخه، رمز عبور هم داریم)

اول عمداً فرم را اشتباه پر می‌کنم:
- نام خالی
- ایمیل نامعتبر
- رمز کوتاه

Submit می‌کنم → پیام‌های خطا کنار هر فیلد نشان داده می‌شود.
این همان Validation سمت سرور است که دیدیم.

حالا فرم را درست پر می‌کنم.
بعد از Submit می‌بینم که به صفحه دیگری منتقل می‌شوم: profile.php
این همان "نمایش نتیجه در صفحه دیگر" است.

در صفحه پروفایل، نام و ایمیل من با استفاده از htmlspecialchars امن و تمیز نمایش داده می‌شود.
```

### 6.3. ربط به سرفصل استاد

اینجا می‌توانی خیلی شفاف جمع‌بندی کنی:

- **ارسال داده با POST** → فرم ثبت‌نام با متد POST
- **دسترسی با `$_POST`** → در `register.php` اطلاعات از `$_POST` خوانده می‌شود
- **اعتبارسنجی داده‌ها** → چک خالی نبودن، فرمت ایمیل، طول رمز عبور
- **جلوگیری از XSS** → در `profile.php` از `htmlspecialchars()` برای نمایش استفاده شده
- **نمایش نتیجه در صفحه دیگر** → بعد از پردازش موفق، ریدایرکت به صفحه پروفایل

---

## 7️⃣ جمع‌بندی و پایان (۲–۳ دقیقه)

**اسکریپت پیشنهادی برای پایان:**

```text
خب، امروز چی یاد گرفتیم؟

1) تفاوت GET و POST و اینکه کی از کدوم استفاده کنیم
2) چطور با $_GET و $_POST به داده‌های فرم دسترسی پیدا کنیم
3) چرا اعتبارسنجی سمت سرور (Validation) خیلی مهم است
4) چطور با یک تابع ساده مثل htmlspecialchars از حملات XSS جلوگیری کنیم
5) و در نهایت، چطور یک فرم ثبت‌نام درست کنیم و نتیجه را در صفحه دیگر نمایش بدهیم

نکته طلایی:
- هیچ‌وقت به ورودی کاربر اعتماد نکنید
- همیشه در سمت سرور اعتبارسنجی کنید
- و همیشه خروجی را قبل از نمایش در HTML امن کنید

اگر سوالی دارید، خیلی خوشحال می‌شم جواب بدم. 🙂
```

---

## 🎯 نکات برای جذاب‌تر کردن ارائه

- **تعامل:** از بچه‌ها بخواه حدس بزنند اگر کد مخرب بنویسیم چه اتفاقی می‌افتد
- **استفاده از DevTools:** یک بار تب Network را باز کن و Request را نشان بده
- **رنگ و UI:** تم Cyberpunk پروژه‌ات خودش ارائه را جذاب می‌کند، فقط حواست باشد خیلی سریع اسکرول نکنی که استاد جا نماند
- **زمان‌بندی:** اگر زمان کم بود، بخش SQL Injection و CSRF و File Upload را کامل حذف کن و فقط روی همین چهارتا تمرکز کن:
  - GET vs POST
  - $_GET و $_POST
  - Validation
  - XSS + فرم ثبت‌نام نهایی

---

## ❓ سوالات احتمالی استاد و جواب‌های آماده

- **سوال:** چرا در مثال‌ها از GET برای نمایش نتیجه استفاده کردی ولی گفتی برای اطلاعات حساس خوب نیست؟
  - **جواب پیشنهادی:**
    - اینجا برای آموزش از GET استفاده کردیم تا مسیر داده در URL را واضح ببینیم.
    - در دنیای واقعی، برای اطلاعات حساس (مثل رمز عبور) از POST + Session + HTTPS استفاده می‌کنیم.
    - همین تفاوت را در ارائه هم توضیح دادم تا مرز آموزش و تولید مشخص باشد.

- **سوال:** اگر جاوااسکریپت کاربر خاموش باشد چه اتفاقی می‌افتد؟
  - **جواب پیشنهادی:**
    - ما Validation اصلی را در سمت سرور انجام دادیم (با PHP)، بنابراین حتی بدون JS هم از داده‌های نامعتبر جلوگیری می‌شود.
    - جاوااسکریپت فقط تجربه کاربری را بهتر می‌کند، ولی امنیت واقعی باید در Backend باشد.

- **سوال:** آیا `htmlspecialchars()` برای همه نوع حملات XSS کافی است؟
  - **جواب پیشنهادی:**
    - برای بسیاری از سناریوها، مخصوصاً وقتی داده را در متن HTML چاپ می‌کنیم، بله خیلی کمک می‌کند.
    - ولی برای جاهای خاص (مثل داخل JavaScript یا Attributeها) باید دقت بیشتری در نوع Encoding داشته باشیم.
    - در پروژه‌های واقعی معمولاً از فریم‌ورک‌هایی مثل Laravel استفاده می‌کنیم که این کار را خودکار انجام می‌دهند.

- **سوال:** چرا از Session برای فرم نهایی استفاده نکردی؟
  - **جواب پیشنهادی:**
    - می‌شد از Session هم استفاده کرد (و در یک نسخه دیگر همین کار را انجام می‌دهم).
    - اما برای این درس و این زمان محدود، هدف این بود که روی مفاهیم فرم و GET/POST و Validation و XSS تمرکز کنیم.
    - استفاده از Query String اینجا کمک می‌کند دانشجوها ببینند داده چطور بین صفحات جابه‌جا می‌شود.

- **سوال:** اگر بخواهیم این فرم را واقعاً در یک سایت استفاده کنیم، چه چیزهایی باید اضافه کنیم؟
  - **جواب پیشنهادی:**
    - ذخیره امن رمز عبور با `password_hash()` در دیتابیس
    - اضافه کردن CSRF Token برای فرم‌ها
    - محدود کردن تعداد تلاش‌ها (Rate Limiting)
    - استفاده از HTTPS اجباری
    - Log کردن خطاها و تلاش‌های مشکوک برای تحلیل امنیتی

---

موفق باشی 🌟
